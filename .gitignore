node_modules/
# Query results are research data, not code; if a run happens to use this
# package folder as its working directory, keep its output out of git.
queries/
papers/
index/
reviews/
chats/
pi-literature-review/
# Output of a run started in this folder (research data, not code)
lit-search/
lit-selection/
lit-synthesis/
# Claude Code session state
.claude/
